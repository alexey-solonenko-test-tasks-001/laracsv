<li class="nav-item">
<a class="nav-link  {{ ($active ?? false) ? 'active' : '' }}" id="{{ $id }}" data-toggle="list" href="{{ $href }}" role="tab"
        aria-controls="{{ $ariaC }}">{{ $label }}</a>
</li>